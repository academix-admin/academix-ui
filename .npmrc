# Scoped packages are published publicly by default (override per-package via publishConfig).
# Provide your token in the environment as NPM_TOKEN when publishing in CI:
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
